function weight = calcWeights(length)
    % in:
    % averageSpeed            : the averageSpeed on that edge
    % length                  : the legth of the edge
    %
    % out:
    % weights                 : the resulting weight of that edge
    
    % the average time to go over the full length of that edge
    
    weight = length;
    % weights = length./averageSpeed; 
end